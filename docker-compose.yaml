services:
  diwali-be:
    image: diwali.azurecr.io/diwali-be-app:1.1.0 # Replace with your Docker image
    build:
      context: .
      dockerfile: Dockerfile
      args:
        ENV_FILE: .env.dev.properties
    ports:
      - "3000:3000"
    restart: always